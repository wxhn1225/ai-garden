# 拾知 · 设计规范

## 页面任务

这个网站的主要任务是查找、阅读和整理。目录页优先让资料容易被扫读，详情页优先让正文舒适易读。用真实内容、编号、留白与轻边界形成身份。

## 唯一浅色主题

- 画布：`#F7F8FA`；内容面：`#FFFFFF`；侧栏：`#FCFCFD`。
- 正文：`#202735`；次要文字：`#647083`；边界：`#E6E9EF`。
- 交互色：`#2859DB`；选中背景：`#EAF0FF`。
- 资料字标允许低饱和蓝、紫、橙、绿、玫瑰与灰；筛选标签保持中性。
- 设置 `color-scheme: light`，不随系统切换，不加入主题切换按钮。

## 布局与排版

桌面使用约 232px 的侧栏；主内容留白 42px。目录采用 3 / 2 / 1 列卡片，自适应小屏。手机导航转为横向可滚动的板块入口，保留完整功能名称。页面先呈现标题、搜索和资料，不做整屏宣传区。

以系统中文无衬线为正文，不依赖远程字体服务。主标题约 32–34px，资源标题约 18px，正文 16px；常规标签和操作约 14px，元信息不少于 12px。中文不用大幅负字距，长标题自然换行。

使用 4 / 8px 间距节奏。卡片圆角约 11px、内边距 20px，输入与按钮约 8–9px。通过背景和结构线分层，阴影只作轻反馈。

## 交互

- 类型、标签与搜索条件同步 URL。高频筛选立即反馈，不做整页入场动画。
- `Ctrl/Cmd K` 或非输入状态下 `/` 聚焦搜索；`Escape` 清空并退出输入。
- 标题链接进入站内详情，单独“访问”打开原始来源，避免嵌套链接。
- 保留可见键盘焦点、跳到正文、输入标签与搜索结果播报。
- 移动端操作尽量达到 44px；不依赖悬停才出现主要功能。
- 空结果提供清除筛选；404 提供返回目录；复制失败显示手动复制提示。
- 轻反馈约 150–160ms，只过渡颜色或阴影；减少动态效果时移除过渡。

## 内容

显示真实条目数与文件日期，不显示虚构评分、热度或学习进度。用途写清楚“可以参考什么”，避免“最强”“必备”等宣传表达。原始收藏、调研整理、本站模板在详情中注明来源类型，不把调研写成个人实测。

## 参考取舍

采用 UI UX Pro Max 的基础可访问性、Anthropic 的内容与视觉结合、Impeccable 的操作/阅读区分、Taste minimalist 的轻边界、Emil 的即时反馈、Vercel 的导航与键盘规则，以及 Stitch 的结构化规范。

其中营销落地页的强动效、超大留白、课程引流与热度背书不进入本产品。完整调研覆盖和访问限制见 `docs/design-research.md`。
