# 拾知 · 设计规范

## 页面任务

这个网站的主要任务是查找、阅读和整理。用户从四份本地 HTML 方案中选择 C「便签工作室」：把资料呈现为摊开在桌面上的纸张，用文件夹组织板块，用便笺保留阅读的温度。目录页仍以查找为先，详情页以正文阅读为先。

## 唯一浅色主题

- 桌面画布：`#F6F3EC`；纸张：`#FFFEFA`；正文：`#34382F`。
- 次要文字：`#666B5E`；边界：`#DFDFD3`；交互强调：`#B54B24`。
- 品牌标记以 `#EDB494` 的桃色底承载四格图形；主标题局部使用陶土色与细下划线。
- 文件夹使用低饱和底色：全部 `#EEE6D9`、网页设计 `#F1D9C8`、Skills `#DFE8D6`、性能优化 `#EEE6B9`、Prompt `#EEDDD9`、笔记 `#E4E5E3`。
- 纸张可以使用淡绿、淡粉与浅黄辅助分组；板块名称、类型和链接同时用文字表达，不只依赖颜色。
- 设置 `color-scheme: light`，不随系统切换，不加入主题切换按钮。

## 布局与排版

全宽工作室页头承载品牌和维护入口，内容居中，最大宽度约 1376px，桌面两侧留白约 54px。首页标题旁放置一张带胶带的浅绿便笺；标题下直接提供搜索。板块导航是横向排列的彩色文件夹页签，与当前文件夹内容面相接。

纸张卡片采用 3 / 2 / 1 列布局；宽屏可用一张跨列卡片形成阅读起点。小屏缩减横向留白，首页便笺让位给标题和搜索，文件夹页签可以横向滚动。首页中文标题分成完整短句，避免最后一个字孤立。卡片长标题自然换行，正文与主要操作保持可读。

以系统中文无衬线为正文，不依赖远程字体服务；英文资料标题和手写感强调使用系统衬线字体。首页主标题约 32–54px，普通纸张标题约 23–25px，正文 16px，标签与元信息不少于 12px。中文标题避免过紧字距。

纸张使用约 3px 小圆角、细边框和轻阴影；卡片内边距约 24px。文件夹与搜索框使用较大圆角，与纸张区分。少量卡片轻微倾斜，便笺通过胶带与自然偏转形成层次。详情页延续奶油色阅读纸张，正文、目录、标签和相关资料有清楚的分区；代码块与表格优先保证可读和可滚动。

## 交互

- 类型、标签与搜索条件同步 URL。高频筛选立即反馈，不做整页入场动画。
- `Ctrl/Cmd K` 或非输入状态下 `/` 聚焦搜索；`Escape` 清空并退出输入。
- 文件夹页签进入对应板块；“筛选”展开类型与标签，排序和视图切换直接显示在资料区。
- 标题及阅读入口进入站内详情，独立来源链接打开原网页，避免嵌套链接。
- 从详情返回资料列表时恢复原来的搜索与筛选条件；直接打开详情也有明确返回入口。
- 保留可见键盘焦点、跳到正文、输入标签与搜索结果播报。
- 移动端操作尽量达到 44px；不依赖悬停才出现主要功能。
- 空结果提供清除筛选；404 提供返回目录；复制失败显示手动复制提示。
- 轻反馈约 160ms，纸张悬停可轻微上移；不做整页入场动画。小屏移除卡片倾斜，减少动态效果偏好下移除过渡和移动反馈。

## 内容

显示真实条目数与文件日期，不显示虚构评分、热度或学习进度。用途写清楚“可以参考什么”，避免“最强”“必备”等宣传表达。原始收藏、调研整理、本站模板在详情中注明来源类型，不把调研写成个人实测。

## 参考取舍

最终视觉以用户选择的 C「便签工作室」本地原型为准。前期调研提供可访问性、内容层级、操作与阅读分区、即时反馈、导航和键盘规则；这些规则应用于文件夹与纸张界面。

其中营销落地页的强动效、超大留白、课程引流与热度背书不进入本产品。完整调研覆盖和访问限制见 `docs/design-research.md`。
